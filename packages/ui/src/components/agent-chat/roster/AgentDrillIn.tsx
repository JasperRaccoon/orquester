/**
 * Agent chat — `AgentDrillIn` placeholder (§7.6).
 *
 * **Stub. Package W14 replaces the body; the props in `contracts.ts` are
 * the contract and do not change.**
 */

import type { AgentDrillInProps } from "../contracts";

export function AgentDrillIn(props: AgentDrillInProps): JSX.Element {
  void props;
  return <div data-agent-chat-stub="AgentDrillIn" />;
}

export default AgentDrillIn;
