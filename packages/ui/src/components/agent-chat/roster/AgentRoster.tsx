/**
 * Agent chat — `AgentRoster` placeholder (§7.6).
 *
 * **Stub. Package W14 replaces the body; the props in `contracts.ts` are
 * the contract and do not change.**
 */

import type { AgentRosterProps } from "../contracts";

export function AgentRoster(props: AgentRosterProps): JSX.Element {
  void props;
  return <div data-agent-chat-stub="AgentRoster" />;
}

export default AgentRoster;
