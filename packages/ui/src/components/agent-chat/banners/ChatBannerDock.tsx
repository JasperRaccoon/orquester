/**
 * Agent chat — `ChatBannerDock` placeholder (§7.5).
 *
 * **Stub. Package W13 replaces the body; the props in `contracts.ts` are
 * the contract and do not change.**
 */

import type { ChatBannerDockProps } from "../contracts";

export function ChatBannerDock(props: ChatBannerDockProps): JSX.Element {
  void props;
  return <div data-agent-chat-stub="ChatBannerDock" />;
}

export default ChatBannerDock;
