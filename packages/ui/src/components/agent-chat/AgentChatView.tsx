/**
 * Agent chat — `AgentChatView` placeholder (§7.1).
 *
 * **Stub. Package W15 replaces the body; the props in `contracts.ts` are
 * the contract and do not change.**
 */

import type { AgentChatViewProps } from "./contracts";

export function AgentChatView(props: AgentChatViewProps): JSX.Element {
  void props;
  return <div data-agent-chat-stub="AgentChatView" />;
}

export default AgentChatView;
