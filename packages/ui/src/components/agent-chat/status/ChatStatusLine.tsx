/**
 * Agent chat — `ChatStatusLine` placeholder (§7.6).
 *
 * **Stub. Package W14 replaces the body; the props in `contracts.ts` are
 * the contract and do not change.**
 */

import type { ChatStatusLineProps } from "../contracts";

export function ChatStatusLine(props: ChatStatusLineProps): JSX.Element {
  void props;
  return <div data-agent-chat-stub="ChatStatusLine" />;
}

export default ChatStatusLine;
