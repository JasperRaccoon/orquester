/**
 * Agent chat — `ChatTimeline` placeholder (§7.3).
 *
 * **Stub. Package W12 replaces the body; the props in `contracts.ts` are
 * the contract and do not change.**
 */

import type { ChatTimelineProps } from "../contracts";

export function ChatTimeline(props: ChatTimelineProps): JSX.Element {
  void props;
  return <div data-agent-chat-stub="ChatTimeline" />;
}

export default ChatTimeline;
