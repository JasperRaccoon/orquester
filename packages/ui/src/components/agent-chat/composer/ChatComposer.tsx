/**
 * Agent chat — `ChatComposer` placeholder (§7.4).
 *
 * **Stub. Package W13 replaces the body; the props in `contracts.ts` are
 * the contract and do not change.**
 */

import type { ChatComposerProps } from "../contracts";

export function ChatComposer(props: ChatComposerProps): JSX.Element {
  void props;
  return <div data-agent-chat-stub="ChatComposer" />;
}

export default ChatComposer;
